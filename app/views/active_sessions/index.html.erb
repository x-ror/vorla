<% content_for(:title) { "Security - Vorla" } %>

<%= render "shared/navbar" %>

<main class="flex-1 bg-muted/50 pb-16 dark:bg-background">
  <div class="mx-auto max-w-3xl px-6 pt-12">

    <p class="text-xs font-medium uppercase tracking-widest text-muted-foreground">Account</p>
    <h1 class="mt-1 text-3xl font-normal tracking-tight">Security</h1>

    <%# ══ Card 1 — Active Sessions ══ %>
    <div class="mt-10 rounded-xl border border-border/80 bg-card shadow-md dark:shadow-none">
      <div class="flex items-center justify-between border-b border-border px-6 py-4">
        <span class="text-xs font-medium uppercase tracking-widest text-muted-foreground">Active sessions</span>
        <% if @sessions.size > 1 %>
          <%= button_to "Revoke all others", destroy_all_active_sessions_path, method: :delete,
            class: "text-xs font-medium text-destructive transition-opacity hover:opacity-70",
            data: { turbo_confirm: "Revoke all other sessions?" } %>
        <% end %>
      </div>

      <div class="divide-y divide-border">
        <% @sessions.each do |sess| %>
          <div class="flex items-center gap-4 px-6 py-4">
            <%# Device icon %>
            <div class="flex h-9 w-9 shrink-0 items-center justify-center rounded-lg border border-border bg-background">
              <svg class="h-4 w-4 text-muted-foreground" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <% if sess.device_name == "iPhone" || sess.device_name == "iPad" || sess.device_name == "Android" %>
                  <rect width="14" height="20" x="5" y="2" rx="2" ry="2"/><line x1="12" x2="12.01" y1="18" y2="18"/>
                <% else %>
                  <rect width="20" height="14" x="2" y="3" rx="2"/><line x1="8" x2="16" y1="21" y2="21"/><line x1="12" x2="12" y1="17" y2="21"/>
                <% end %>
              </svg>
            </div>

            <%# Info %>
            <div class="min-w-0 flex-1">
              <div class="flex items-center gap-2">
                <p class="text-sm font-medium"><%= sess.browser_name %> on <%= sess.device_name %></p>
                <% if sess == @current_session %>
                  <span class="rounded bg-primary/10 px-2 py-0.5 text-xs font-bold uppercase tracking-wider text-primary">This device</span>
                <% end %>
              </div>
              <p class="text-xs text-muted-foreground"><%= sess.ip_address %> &middot; <%= sess == @current_session ? "Active now" : "#{time_ago_in_words(sess.created_at)} ago" %></p>
            </div>

            <%# Action %>
            <% if sess == @current_session %>
              <span class="rounded bg-primary/10 px-2 py-0.5 text-xs font-bold uppercase tracking-wider text-primary">Active</span>
            <% else %>
              <%= button_to active_session_path(sess), method: :delete,
                class: "text-xs font-medium text-destructive/70 transition-colors hover:text-destructive",
                data: { turbo_confirm: "Revoke this session?" } do %>
                Revoke
              <% end %>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>

    <%# ══ Quick actions ══ %>
    <div class="mt-8 flex flex-wrap items-center gap-3" data-controller="passkey">
      <button type="button" data-action="click->passkey#register" class="inline-flex items-center gap-2 rounded-lg border border-border bg-card px-4 py-2.5 text-xs font-medium text-muted-foreground shadow-sm transition-colors hover:text-foreground">
        <svg class="h-4 w-4" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-fingerprint-pattern-icon lucide-fingerprint-pattern"><path d="M12 10a2 2 0 0 0-2 2c0 1.02-.1 2.51-.26 4"/><path d="M14 13.12c0 2.38 0 6.38-1 8.88"/><path d="M17.29 21.02c.12-.6.43-2.3.5-3.02"/><path d="M2 12a10 10 0 0 1 18-6"/><path d="M2 16h.01"/><path d="M21.8 16c.2-2 .131-5.354 0-6"/><path d="M5 19.5C5.5 18 6 15 6 12a6 6 0 0 1 .34-2"/><path d="M8.65 22c.21-.66.45-1.32.57-2"/><path d="M9 6.8a6 6 0 0 1 9 5.2v2"/></svg>
        Add a passkey
      </button>

      <%= link_to new_password_path, class: "inline-flex items-center gap-2 rounded-lg border border-border bg-card px-4 py-2.5 text-xs font-medium text-muted-foreground shadow-sm transition-colors hover:text-foreground" do %>
        <svg class="h-4 w-4" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="18" height="11" x="3" y="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
        Reset password
      <% end %>

      <p class="text-xs text-muted-foreground" data-passkey-target="status"></p>
    </div>

    <% if @passkey_credentials.any? %>
      <div class="mt-4 space-y-2">
        <% @passkey_credentials.each do |cred| %>
          <div class="flex items-center gap-3 rounded-lg border border-border bg-card px-4 py-3 shadow-sm">
            <svg class="h-4 w-4 shrink-0 text-muted-foreground" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 10v4"/><path d="M7.5 7.5c.7-.7 1.5-1 2.5-1a3.5 3.5 0 0 1 0 7H9"/><path d="M2 12a10 10 0 1 0 20 0 10 10 0 1 0-20 0"/></svg>
            <span class="flex-1 text-sm font-medium"><%= cred.nickname %></span>
            <span class="text-xs text-muted-foreground"><%= time_ago_in_words(cred.created_at) %> ago</span>
            <%= button_to passkey_path(cred), method: :delete,
              class: "text-xs font-medium text-destructive/70 transition-colors hover:text-destructive",
              data: { turbo_confirm: "Remove this passkey?" } do %>
              Remove
            <% end %>
          </div>
        <% end %>
      </div>
    <% end %>

  </div>
</main>

<%= render "shared/footer" %>
