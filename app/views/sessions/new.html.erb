<% content_for(:title) { "Sign In - Vorla" } %>

<div class="flex min-h-screen items-center justify-center px-4 py-10">
  <div class="w-full max-w-sm animate-slide-up">
    <%= render_card(class: "w-full") do %>
      <div class="p-8">
        <div class="mb-8 text-center">
          <a href="/" class="inline-flex h-12 w-12 items-center justify-center rounded-lg bg-primary text-primary-foreground font-extrabold text-xl mb-6">V</a>
          <h1 class="text-xl font-extrabold tracking-tight">Welcome back</h1>
          <p class="mt-1 text-sm text-muted-foreground">Sign in to access your account</p>
        </div>

        <%= form_with url: session_path, class: "space-y-5" do |f| %>
          <% if flash[:alert] %>
            <%= render_alert title: "Error", description: flash[:alert], variant: :destructive %>
          <% end %>

          <div class="space-y-1.5">
            <label class="text-sm font-medium">Email</label>
            <%= render_input name: "email_address", type: :email, placeholder: "you@example.com", autocomplete: "email", required: true %>
          </div>

          <div class="space-y-1.5">
            <label class="text-sm font-medium">Password</label>
            <%= render_input name: "password", type: :password, placeholder: "Your password", autocomplete: "current-password", required: true %>
          </div>

          <%= render_button "Sign In →", class: "w-full" %>
        <% end %>

        <div class="mt-6 text-center text-sm text-muted-foreground">
          Don't have an account? <a href="<%= signup_path %>" class="font-semibold text-primary hover:underline">Sign up</a>
        </div>
      </div>
    <% end %>
  </div>
</div>
